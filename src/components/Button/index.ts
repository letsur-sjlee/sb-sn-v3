/**
 * Button 컴포넌트 export
 *
 * 이 파일은 Button 컴포넌트와 관련 타입을 외부에 공개합니다.
 * import 경로를 간결하게 유지하기 위해 index.ts를 통해 re-export합니다.
 *
 * 사용 예시:
 * import { Button, type ButtonProps } from "@/components/Button";
 */
export { Button, type ButtonProps } from "./Button";
